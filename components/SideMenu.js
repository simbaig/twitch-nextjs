import React from "react";
import { rec_channels, top_channels } from "../data/data";

const SideMenu = () => {
	return (
		<div className="fixed w-14 sm:w-16 xl:w-[15rem] h-screen p-2 bg-[#0e0e10]"></div>
	);
};

export default SideMenu;
